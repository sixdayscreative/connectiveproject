<% include ../partials/header %>

<div class="container">
  <h1>Edit Comment</h1>

  <form action="/songs/<%= song_id %>/comments/<%= comment.id %>?_method=PUT" method="POST">
  Posting a comment as <a href="#"><%= currentUser.username %></a> - Not you? <a href="/logout">Log out</a>
  <div class="form-group">
    <textarea class="form-control" name="comment[text]" cols="30" rows="10" placeholder="Your Comment"><%= comment.text %></textarea>
  </div>
  <div class="form-group">
    <button class="btn btn-primary">Add Comment</button>
  </div>
  </form>
  </div>
<% include ../partials/footer %>
